# گزارش مرحله ۱۹ — چرخه اصلاح ورود سند و پردازش واقعی

## هدف
رفع مشکل مرحله قبلی که فایل فقط وارد صف می‌شد و هیچ‌وقت وارد پایگاه دانش نمی‌شد.

## تغییرات اصلی
- پایگاه دانش داخلی از حالت فقط‌خواندنی خارج شد و پس از کپی اولیه asset، به‌صورت writable نگهداری می‌شود.
- پردازش واقعی پس‌زمینه با Android WorkManager اضافه شد.
- پس از انتخاب فایل، پردازش صف به‌صورت خودکار زمان‌بندی می‌شود.
- اجرای هم‌زمان چند پردازش صف با `UniqueWork + KEEP` کنترل شده است.
- SHA-256 برای جلوگیری از ورود تکراری استفاده می‌شود.
- PDF متنی با PDFBox استخراج می‌شود؛ هر صفحه به‌صورت مستقل پردازش و سپس در صورت طول زیاد به chunk تقسیم می‌شود.
- TXT مستقیماً استخراج و chunk می‌شود.
- DOCX بدون وابستگی سنگین به POI، از `word/document.xml` استخراج می‌شود.
- XLSX از `sharedStrings.xml` و worksheetهای استاندارد استخراج می‌شود.
- پس از درج موفق، FTS5 با `rebuild` همگام می‌شود.
- فایل فقط بعد از درج موفق در DB حذف می‌شود؛ در خطا در صف باقی می‌ماند.
- PDF اسکن‌شده/تصویری که متن قابل استخراج ندارد وارد دانش نمی‌شود و برای OCR/بررسی باقی می‌ماند.
- نسخه برنامه به `1.1.0` و `versionCode=16` ارتقا یافت.
- Workflow برای شاخه‌های `master` و `main` اصلاح شد و قبل از build، `clean` اجرا می‌شود.

## وابستگی‌های جدید
- `androidx.work:work-runtime:2.11.2`
- `com.tom-roush:pdfbox-android:2.0.27.0`

## آزمون‌های انجام‌شده در محیط بررسی
- بررسی schema واقعی SQLite و سازگاری جدول `chunks` و FTS5.
- آزمون درج رکورد آزمایشی و `FTS5 rebuild` روی کپی DB واقعی؛ جست‌وجوی فارسی رکورد تازه‌وارد موفق بود.
- بررسی توازن syntax در فایل‌های Kotlin جدید/تغییر‌یافته.
- بررسی XMLهای Manifest/Layout/Styles با parser XML.
- بررسی YAML workflow با parser YAML.
- بررسی وضعیت Git و فایل‌های تغییر یافته.

## محدودیت مهم محیط فعلی
این محیط Android SDK/Gradle اجرایی ندارد؛ بنابراین APK نهایی را در همین محیط نمی‌توانم build و روی گوشی اجرا کنم. Build نهایی باید در GitHub Actions انجام شود. این محدودیت عمداً پنهان نمی‌شود و نتیجه build GitHub معیار نهایی است.
